# 微信视频号直播数据爬虫项目使用说明

## 环境要求

- Python 3.12 

## 常用命令
```bash
# 删除现有虚拟环境
rm -rf .venv

# 创建新的虚拟环境
python -m venv .venv

# 激活虚拟环境
source .venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 启动
python crawler.py
```
